# 未決事項

このファイルには、まだ決まっていないこと、実装前または初期実装中に確認することを集約します。

## 認証・運用

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| LAN 内アクセスの HTTPS 化 | 初期は HTTP も許容し、将来的に家庭内 HTTPS 化を検討 | 未決 |
| 家族ユーザーの追加方法 | 管理者が `ALLOWED_CLOUDFLARE_EMAILS` を設定し、Cloudflare Access 経由で初回ログイン | 仮決定（実装済み） |
| Cloudflare Access JWT 検証 | 初期実装から入れるのが望ましい | 未実装 |
| Secret 管理 | GCP Secret Manager を原本とし、ESO で Kubernetes Secret に同期 | 仮決定 |
| バックアップ運用 | PostgreSQL + ファイルストレージ（本番は JuiceFS 側） | 仮決定 |

## 買い物リスト

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| リスト共有範囲 | 家族内で複数リスト。デフォルトリストを1つ指定 | **決定** |
| リスト完了後の新リスト | 手動で新規作成（自動作成しない） | 仮決定 |
| マスター削除時の既存アイテム | `master_id` を NULL にし表示名は維持 | 仮決定 |
| 初回デフォルトリスト | 初回アクセス時に「買い物」リストを自動作成 | 仮決定 |
| マスター編集権限 | 初期は全員編集可。将来 admin のみに制限するか検討 | 未決 |

## Home / UI

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| クイックアクセス | 初期はコード固定。将来カスタマイズ | **決定** |
| ダッシュボード指標 | 買い物リストの未完了件数・デフォルトリスト・最終完了日 | 仮決定 |

## ファイルストレージ（アプリ側）

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| アプリの I/O 方式 | POSIX ファイルパス（`FILE_STORAGE_ROOT`） | **決定** |
| 開発環境 | ローカルディレクトリ（JuiceFS なし） | **決定** |
| 本番環境 | 家庭 k3s 管理リポジトリの JuiceFS PVC をマウント | **決定** |
| 画像最大サイズ | 5 MB | 仮決定 |
| 許可 MIME | jpeg, png, webp | 仮決定 |

JuiceFS の構築詳細（メタ DB、オブジェクト層、CSI など）は家庭 k3s 管理リポジトリで決定します。本リポジトリでは未決としません。

## 本番環境

| 項目 | 状態 |
| --- | --- |
| 本番 DB 名 | 未決 |
| 本番 namespace | 未決 |
| JuiceFS PVC 名（Helm `existingClaim`） | 未決（k3s 管理リポジトリと連携） |
| Cloudflare Tunnel 設定 | 未決 |

## 解決済み（参考）

| 項目 | 決定内容 |
| --- | --- |
| アプリの主要機能 | 家族 Hub + 買い物リスト（フェーズ 1） |
| 画面構成 | ヘッダー + コンテンツ、ハンバーガーメニュー |
| バイナリ保存 | `FILE_STORAGE_ROOT` 配下のファイル I/O。本番は JuiceFS PVC |

## 確認が必要なとき

機能仕様の更新後、決定した項目はこのファイルから削除または「決定済み」へ移してください。
