# 未決事項

このファイルには、まだ決まっていないこと、実装前または初期実装中に確認することを集約します。

## 認証・運用

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| LAN 内アクセスの HTTPS 化 | 初期は HTTP も許容し、将来的に家庭内 HTTPS 化を検討 | 未決 |
| 家族ユーザーの追加方法 | 管理者が `ALLOWED_CLOUDFLARE_EMAILS` を設定し、Cloudflare Access 経由で初回ログイン | 仮決定（実装済み） |
| 管理者人数 | 初期は 1 名。必要になったら追加 | 仮決定 |
| パスワードリセット | 管理者がリセット要求を発行し、本人が再設定 | 未実装 |
| Cloudflare Access JWT 検証 | 初期実装から入れるのが望ましい | 未実装 |
| Secret 管理 | GCP Secret Manager を原本とし、ESO で Kubernetes Secret に同期 | 仮決定 |
| バックアップ運用 | 既存 PostgreSQL 側のバックアップ対象に専用 DB を含める | 仮決定 |

## アプリケーション機能

| 項目 | 状態 |
| --- | --- |
| アプリで扱う具体的な機能 | 未決 |
| 画面構成（業務画面） | 未決 |
| 通常ユーザーと管理者の権限差分（業務機能） | 未決 |

## 本番環境

| 項目 | 状態 |
| --- | --- |
| 本番 DB 名 | 未決 |
| 本番 DB 接続ユーザー名 | 未決 |
| 本番 namespace | 未決 |
| GCP Secret Manager の secret 命名規則 | 未決 |
| Cloudflare Tunnel 設定 | 未決 |

## 確認が必要なとき

アプリの主要機能を決める際は、`application/design.md` と `application/features/` を更新し、確定した項目はこのファイルから削除または「決定済み」へ移してください。
