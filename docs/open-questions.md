# 未決事項

このファイルには、まだ決まっていないこと、実装前または初期実装中に確認することを集約します。

## 認証・運用

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| LAN 内アクセスの HTTPS 化 | 初期は HTTP も許容し、将来的に家庭内 HTTPS 化を検討 | 未決 |
| Cloudflare Access JWT 検証 | フェーズ3で実装 | **決定** |
| CSRF 対策 | フェーズ1は SameSite=Lax のみ | **決定** |
| Secret 管理 | GCP Secret Manager を原本とし、ESO で Kubernetes Secret に同期 | 仮決定 |
| バックアップ運用 | PostgreSQL + ファイルストレージ（本番は JuiceFS 側） | 仮決定 |

## グループ

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| 個人グループ | ユーザー作成時に自動作成 | **決定** |
| 共有グループ（フェーズ1） | 管理者が手動作成・メンバー追加 | **決定** |
| 招待 API | フェーズ2 | **決定** |
| 買い物リストのデフォルト group_id | 省略時は個人グループ。共有は UI で切替 | **決定** |

## 買い物リスト

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| データスコープ | `group_id` 単位 | **決定** |
| リスト状態 | `active` / `archived` のみ（完了状態なし） | **決定** |
| 履歴（スナップショット） | フェーズ1では不要 | **決定** |
| デフォルトリスト | グループごとに1つ。自動作成しない | **決定** |
| 表示順 | GET の `sort` クエリのみ。view はフェーズ2 | **決定** |
| 同時編集 | Last-Write-Wins | **決定** |
| マスター削除時の既存アイテム | `master_id` を NULL にし表示名は維持 | **決定** |
| マスター編集権限 | グループメンバー全員が CRUD 可 | **決定** |

## Home / UI

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| クイックアクセス | 初期はコード固定。将来カスタマイズ | **決定** |
| ダッシュボード API | `/api/v1/shopping/summary` のみ | **決定** |
| パスワード設定画面 | `/settings/password` | **決定** |

## ファイルストレージ（アプリ側）

| 項目 | 推奨初期判断 | 状態 |
| --- | --- | --- |
| 取得 API | `GET /api/v1/files/{id}` | **決定** |
| 画像 GET 認可 | 認証済み user/admin 全員 | **決定** |
| マスター削除時のファイル | stored_objects + 実ファイルも削除 | **決定** |

JuiceFS の構築詳細は家庭 k3s 管理リポジトリで決定します。

## 本番環境

| 項目 | 状態 |
| --- | --- |
| 本番 DB 名 | 未決 |
| 本番 namespace | 未決 |
| JuiceFS PVC 名（Helm `existingClaim`） | 未決（k3s 管理リポジトリと連携） |
| Cloudflare Tunnel 設定 | 未決 |

## 解決済み（参考）

| 項目 | 決定内容 |
| --- | --- |
| Cloudflare メール制限 | アプリでは行わない。CF Access に任せる |
| バイナリ保存 | `FILE_STORAGE_ROOT` 配下。本番は JuiceFS PVC |
| 画面構成 | ヘッダー + コンテンツ、認証済み `/` → `/home` |

## 確認が必要なとき

機能仕様の更新後、決定した項目はこのファイルから削除または「決定済み」へ移してください。
